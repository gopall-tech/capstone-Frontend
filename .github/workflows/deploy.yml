name: Build and Deploy Frontend

on:
  push:
    branches:
      - main
      - develop
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        environment: [dev, qa, prod]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set environment variables
      run: |
        if [ "${{ matrix.environment }}" == "dev" ]; then
          echo "ACR_NAME=gopaldevacr" >> $GITHUB_ENV
          echo "RESOURCE_GROUP=rg-gopal-dev" >> $GITHUB_ENV
          echo "AKS_CLUSTER=aks-gopal-dev" >> $GITHUB_ENV
          echo "NGINX_CONF=nginx.dev.conf" >> $GITHUB_ENV
        elif [ "${{ matrix.environment }}" == "qa" ]; then
          echo "ACR_NAME=gopalqaacr" >> $GITHUB_ENV
          echo "RESOURCE_GROUP=rg-gopal-qa" >> $GITHUB_ENV
          echo "AKS_CLUSTER=aks-gopal-qa" >> $GITHUB_ENV
          echo "NGINX_CONF=nginx.qa.conf" >> $GITHUB_ENV
        else
          echo "ACR_NAME=gopalprodacr" >> $GITHUB_ENV
          echo "RESOURCE_GROUP=rg-gopal-prod" >> $GITHUB_ENV
          echo "AKS_CLUSTER=aks-gopal-prod" >> $GITHUB_ENV
          echo "NGINX_CONF=nginx.prod.conf" >> $GITHUB_ENV
        fi

    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Get ACR login server
      run: |
        ACR_LOGIN_SERVER=$(az acr show --name ${{ env.ACR_NAME }} --query loginServer --output tsv)
        echo "ACR_LOGIN_SERVER=$ACR_LOGIN_SERVER" >> $GITHUB_ENV

    - name: Build image version
      run: |
        SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
        echo "IMAGE_TAG=${{ matrix.environment }}-${SHORT_SHA}" >> $GITHUB_ENV

    - name: Build and push Docker image to ACR
      run: |
        az acr build \
          --registry ${{ env.ACR_NAME }} \
          --image frontend:${{ env.IMAGE_TAG }} \
          --image frontend:${{ matrix.environment }}-latest \
          --build-arg NGINX_CONF=${{ env.NGINX_CONF }} \
          --file Dockerfile \
          .

    - name: Get AKS credentials
      run: |
        az aks get-credentials \
          --resource-group ${{ env.RESOURCE_GROUP }} \
          --name ${{ env.AKS_CLUSTER }} \
          --overwrite-existing

    - name: Deploy to AKS
      run: |
        kubectl set image deployment/frontend \
          frontend=${{ env.ACR_LOGIN_SERVER }}/frontend:${{ env.IMAGE_TAG }} \
          -n gopal-app

    - name: Wait for rollout
      run: |
        kubectl rollout status deployment/frontend -n gopal-app --timeout=5m

    - name: Verify deployment
      run: |
        kubectl get pods -n gopal-app -l app=frontend
        kubectl get ingress -n gopal-app
